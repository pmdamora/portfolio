import React, { FunctionComponent } from 'react';

import { SocialButton } from '@portfolio/components/ui/Footer';
import { Box } from '@portfolio/components/shared/Box';

const SocialLinks: FunctionComponent<{}> = () => {
  return (
    <Box>
      <SocialButton />
    </Box>
  );
};

export { SocialLinks };
